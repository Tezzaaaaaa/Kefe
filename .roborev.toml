agent = "codex"
review_guidelines = """
KEFE is a browser-based lyric/video editor. Treat runtime correctness and user workflow integrity as higher priority than cosmetic refactors.

Review every change for:
- broken browser runtime or undefined globals
- regressions in editor state, playback, captions, lyrics timing, effects, backgrounds, export, and wizard paths
- duplicated/conflicting CSS shell geometry
- event-listener leaks, race conditions, async failures, and missing error handling
- dead code, unreachable code, accidental overrides, and inconsistent module boundaries
- accessibility and responsive-layout regressions
- security issues, especially secrets, unsafe DOM injection, filesystem assumptions, auth/billing mistakes, and server trust-boundary errors

Never make a speculative refactor just to make code look cleaner. Prefer the smallest coherent fix that improves reliability. After fixes, require the repository checks and browser smoke tests to pass.
"""
review_md_fallback = true
