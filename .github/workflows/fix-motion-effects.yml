name: KEFE motion effects integration (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — retained only for history; use the canonical effect integration."